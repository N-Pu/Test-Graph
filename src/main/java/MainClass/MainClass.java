package MainClass;

import GraphPackage.GraphA;

public class MainClass {
    public static void main(String[] args) {
        GraphA g = new GraphA();
        g.Method();
        g.getGen().hashCode(); // delete later
    }
}
